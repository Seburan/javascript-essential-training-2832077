class Pencil {
  constructor(color, eraseable, mechanical, pointType) {
    this.color = color;
    this.eraseable = eraseable;
    this.mechanical = mechanical;
    this.pointType = pointType;
  }
}

export default Pencil;
